[project]
name = "api-handlers"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["shared"]

[tool.uv.sources]
shared = { workspace = true }

[tool.setuptools]
py-modules = ["create_project", "start_pipeline", "get_project", "get_stages", "get_artifact"]
