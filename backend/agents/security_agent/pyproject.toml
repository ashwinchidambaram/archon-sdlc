[project]
name = "security-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["shared", "bandit>=1.7.0"]

[tool.uv.sources]
shared = { workspace = true }

[tool.setuptools]
py-modules = ["handler", "prompt"]
